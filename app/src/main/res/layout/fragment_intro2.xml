<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF3B30">

    <!-- Título principal -->
    <TextView
        android:id="@+id/txt_swap_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Swap Your\nCar"
        android:textColor="@android:color/white"
        android:textSize="42sp"
        android:fontFamily="sans-serif-black"
        android:layout_marginStart="30dp"
        android:layout_marginTop="200dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- Subtítulo -->
    <TextView
        android:id="@+id/txt_swap_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Find, choose and connect"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:layout_marginStart="30dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/txt_swap_title"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Imagen 'porscheback' pegada al lado izquierdo -->
    <ImageView
        android:id="@+id/img_porsche_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/porscheback"
        android:scaleType="fitStart"
        android:adjustViewBounds="true"
        android:layout_marginTop="115dp"
        app:layout_constraintTop_toBottomOf="@id/txt_swap_subtitle"
        app:layout_constraintStart_toStartOf="parent"/>
    <!-- Botón Skip -->

    <Button
        android:id="@+id/btn_skip"
        android:layout_width="100dp"
        android:layout_height="48dp"
        android:text="Skip"
        android:textColor="@android:color/white"
        android:background="@drawable/btn_transparent_2"
        android:backgroundTint="@null"
        app:backgroundTint="@null"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="24dp"
        android:layout_marginBottom="24dp" />

    <!-- Botón Next -->
    <Button
        android:id="@+id/btn_next"
        android:layout_width="100dp"
        android:layout_height="48dp"
        android:text="Next"
        android:textColor="@android:color/white"
        android:background="@drawable/btn_transparent_2"
        android:backgroundTint="@null"
        app:backgroundTint="@null"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
